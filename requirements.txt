sqlalchemy>=2.0
pymysql>=1.1           # MySQL driver — only needed when DATABASE_URL points at hosted MySQL (Aiven)
cryptography>=42.0     # required by pymysql for MySQL 8 caching_sha2 auth
certifi>=2024.2        # CA bundle for the hosted-MySQL TLS connection
pydantic>=2.7
python-dotenv>=1.0
anthropic>=0.69
mcp>=1.20,<2.0     # 2.0 removed mcp.server.fastmcp (renamed/moved to mcp.server.mcpserver.MCPServer)
streamlit>=1.56
fastapi>=0.115         # standalone HTTP backend (backend/main.py) — not used by the in-process Streamlit deploy
uvicorn>=0.30          # ASGI server to run backend/main.py
