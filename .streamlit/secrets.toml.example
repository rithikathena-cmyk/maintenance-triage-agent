# Streamlit secrets template.
#
# For LOCAL testing: copy to `.streamlit/secrets.toml` and fill in.
# For Streamlit Community Cloud: paste the same content into the app's
#   Settings → Secrets box (do NOT commit the real file).
#
# `.streamlit/secrets.toml` is gitignored — never commit real values.

# Public URL of the FastAPI backend deployed on Render.
BACKEND_URL = "https://maintenance-triage-api.onrender.com"
